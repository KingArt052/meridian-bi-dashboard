# Leave unset for local dev and single-host Docker Compose deployments
# (relative /api calls are proxied to the backend automatically).
# Set this for split deployments, e.g. frontend on Vercel + backend on Render:
VITE_API_BASE_URL=https://your-backend.example.com
